# Playbooks UI Prototype

Open `index.html` in a browser. The prototype contains:

- Playbooks catalog;
- Playbook detail;
- tabs for steps, bindings, controls, versions and executions;
- fullscreen Playbook Designer;
- element palette, canvas, inspector and simulation;
- Runtime Profiles instead of Agents.
