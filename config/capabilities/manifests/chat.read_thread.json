{
  "capability_id": "chat.read_thread",
  "version": "1.0",
  "manifest_version": 1,
  "name": "Chat Read Thread",
  "description": "Чтение Slack-треда / сообщения как контекста задачи.",
  "group": "messenger",
  "status": "published",
  "operation_type": "read",
  "risk_level": "low",
  "idempotent": true,
  "cacheable": true,
  "requires_approval": false,
  "allowed_execution_modes": [
    "draft",
    "change_proposal",
    "autonomous"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "channel_id",
      "thread_ts"
    ],
    "properties": {
      "channel_id": {
        "type": "string"
      },
      "thread_ts": {
        "type": "string"
      }
    }
  },
  "output_schema": {
    "type": "object"
  },
  "tags": [
    "slack",
    "messenger",
    "intake"
  ],
  "implementations": [
    {
      "id": "slack.read_thread",
      "type": "mcp",
      "server_id": "slack",
      "tool_name": "slack_reply_to_thread",
      "status": "approved",
      "priority": 100,
      "risk_level": "low",
      "enabled": true,
      "timeout_seconds": 20,
      "notes": "Ingress читает тред через slack_gateway; MCP tool — publish path."
    }
  ],
  "required_input_fields": [
    "channel_id",
    "thread_ts"
  ],
  "visibility": {
    "tenants": [
      "*"
    ],
    "roles": [
      "*"
    ],
    "skills": [
      "*"
    ],
    "playbooks": [
      "business-requirements",
      "system-requirements"
    ]
  }
}
